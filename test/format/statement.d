if (condition) // comment!
	foo();

if (condition) {
	foo();
} // comment!
else {
	bar();
}

switch (x) do {
	case 1:
		break;

	static if (c) {
			enum A = 3;

		case 2: {
			doSomething();
		}

			break;

		case 3:
			foobar();
	}
} while (y);

for (; popFront(), i; ++i, foo()) {}
